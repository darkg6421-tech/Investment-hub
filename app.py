from flask import Flask, request, jsonify\n\napp = Flask(__name__)\n\n@app.route('/sip', methods=['POST'])\ndef calculate_sip():\n    data = request.get_json()\n    amount = data['amount']\n    rate_of_return = data['rate_of_return']\n    years = data['years']\n    total_amount = amount * (((1 + (rate_of_return / 100)) ** (years * 12) - 1) / (rate_of_return / 100))\n    return jsonify({'total_amount': total_amount})\n\n@app.route('/swp', methods=['POST'])\ndef calculate_swp():\n    data = request.get_json()\n    amount = data['amount']\n    rate_of_return = data['rate_of_return']\n    years = data['years']\n    total_amount = amount * ((1 + (rate_of_return / 100)) ** years)\n    return jsonify({'total_amount': total_amount})\n\n@app.route('/stp', methods=['POST'])\ndef calculate_stp():\n    data = request.get_json()\n    amount = data['amount']\n    rate_of_return = data['rate_of_return']\n    years = data['years']\n    total_amount = amount * ((1 + (rate_of_return / 100)) ** (years * 12) - 1) / (rate_of_return / 100)\n    return jsonify({'total_amount': total_amount})\n\n@app.route('/lumpsum', methods=['POST'])\ndef calculate_lumpsum():\n    data = request.get_json()\n    amount = data['amount']\n    rate_of_return = data['rate_of_return']\n    years = data['years']\n    total_amount = amount * (1 + (rate_of_return / 100)) ** years\n    return jsonify({'total_amount': total_amount})\n\nif __name__ == '__main__':\n    app.run(debug=True)